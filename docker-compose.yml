services:
  backend:
    image: tuna99/openshorts-backend-dev
    container_name: tuna99/openshorts-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - backend_models:/app/models
      - ./uploads:/app/uploads
      - ./output:/app/output
      - ./outputs:/app/outputs
      - ./temp:/app/temp
      - ./clips:/app/clips
    environment:
      - PATH=/opt/venv/bin:$PATH
      - YOLO_MODEL_PATH=/app/models/yolov8n.pt
      - MPLCONFIGDIR=/tmp/matplotlib
      - XDG_CACHE_HOME=/tmp/cache
      - ULTRALYTICS_CONFIG_DIR=/tmp/Ultralytics
      - TRANSCRIPTION_PROVIDER=${TRANSCRIPTION_PROVIDER:-local}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_TRANSCRIPTION_MODEL=${GROQ_TRANSCRIPTION_MODEL:-whisper-large-v3-turbo}
      - GROQ_TRANSCRIPTION_BASE_URL=${GROQ_TRANSCRIPTION_BASE_URL:-https://api.groq.com/openai/v1}
      - TRANSCRIPTION_CHUNK_SECONDS=${TRANSCRIPTION_CHUNK_SECONDS:-600}
      - LLM_PROVIDER=${LLM_PROVIDER:-gemini}
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_BASE_URL=${LLM_BASE_URL:-}
      - LLM_MODEL=${LLM_MODEL:-gemini-2.5-flash}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_BASE_URL=${GEMINI_BASE_URL:-}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
    env_file:
      - .env
    restart: unless-stopped

  frontend:
    image: tuna99/openshorts-frontend
    container_name: tuna99/openshorts-frontend
    ports:
      - "5175:5173"
    volumes:
      - ./dashboard:/app
      - /app/node_modules
    restart: unless-stopped
    depends_on:
      - backend

  renderer:
    image: tuna99/openshorts-renderer
    container_name: tuna99/openshorts-renderer
    ports:
      - "3100:3100"
    volumes:
      - ./output:/output
    environment:
      - REMOTION_BUNDLE_PATH=/app/remotion
      - OUTPUT_DIR=/output
      - PORT=3100
    restart: unless-stopped
    depends_on:
      - backend

volumes:
  backend_models:
